# Chronos-T5 baseline benchmark -- Python dependencies.
#
# Pinned to the versions of the original `chronos_bench` environment
# (Python 3.11). These are the ACTUAL imports of the benchmark code; pip resolves
# the rest (transformers/accelerate are pulled in by chronos-forecasting, etc.).
# NOTE: autogluon is NOT required -- "autogluon/fev_datasets" is a HuggingFace
# dataset repo, loaded via the `datasets` library, not the autogluon package.
#
# ---------------------------------------------------------------------------
# STEP 1 -- install PyTorch FIRST, with the CUDA build matching your GPU.
# The pinned build below is CUDA 12.8; pick the index URL for your CUDA version
# from https://pytorch.org/get-started/locally/ . Examples:
#   GPU (CUDA 12.8): pip install torch==2.9.1 --index-url https://download.pytorch.org/whl/cu128
#   CPU-only:        pip install torch==2.9.1
#
# STEP 2 -- install the rest:  pip install -r requirements.txt
# ---------------------------------------------------------------------------

# Core forecasting + data
chronos-forecasting==2.2.2
gluonts==0.16.2
datasets==4.0.0
transformers==4.57.6
accelerate==1.14.0

# Numerics / IO
numpy==2.1.3
pandas==2.3.3
pyarrow==20.0.0
pyyaml==6.0.3

# Notebook generation + execution (make_notebook.py / the deliverable .ipynb)
nbformat==5.10.4
nbconvert==7.17.1
ipykernel==7.3.0
matplotlib==3.10.9
