# Chronos-2 zero-shot study -- Python dependencies.
#
# Mirrors environment.yml exactly (same pins) -- use either; keep them in lock-step.
# Identical to the sibling Chronos-T5 benchmark (Python 3.11): the same
# `chronos-forecasting==2.2.2` wheel ships BOTH the Chronos-T5 (`chronos.chronos`,
# `chronos.chronos_bolt`) and the Chronos-2 (`chronos.chronos2`) pipelines, so no
# extra package is needed -- Chronos-2 is loaded via the same
# `chronos.BaseChronosPipeline.from_pretrained("amazon/chronos-2")` entry point.
#
# ---------------------------------------------------------------------------
# STEP 1 -- install PyTorch FIRST, with the CUDA build matching your GPU:
#   GPU (CUDA 12.8): pip install torch==2.9.1 --index-url https://download.pytorch.org/whl/cu128
#   CPU-only:        pip install torch==2.9.1
# STEP 2 -- install the rest:  pip install -r requirements.txt
# ---------------------------------------------------------------------------

# Core forecasting + data
chronos-forecasting==2.2.2
gluonts==0.16.2
datasets==4.0.0
transformers==4.57.6
accelerate==1.14.0

# Numerics / IO
numpy==2.1.3
pandas==2.3.3
pyarrow==20.0.0
pyyaml==6.0.3
scipy==1.16.3

# Notebook generation + execution (make_notebook.py / the deliverable .ipynb)
nbformat==5.10.4
nbconvert==7.17.1
ipykernel==7.3.0
matplotlib==3.10.9