# Chronos-2 vs Chronos-T5 head-to-head study -- pinned dependencies.
#
# Mirrors environment.yml exactly. Python 3.11.
#
# The single `chronos-forecasting==2.2.2` wheel ships BOTH Chronos-T5 and Chronos-2;
# there is no separate package. Chronos-Bolt also comes from it, which matters for
# the planned third-model experiment.
#
# PyTorch is NOT pinned here because the right wheel depends on your CUDA version.
# Install it first, choosing ONE of:
#   GPU (CUDA 12.8): pip install torch==2.9.1 --index-url https://download.pytorch.org/whl/cu128
#   CPU-only:        pip install torch==2.9.1
# All results in this branch were produced with torch 2.9.1+cu128 on an RTX 3500 Ada.
#
#   pip install -r requirements.txt

# Core forecasting + data
chronos-forecasting==2.2.2
gluonts==0.16.2
datasets==4.0.0
transformers==4.57.6
accelerate==1.14.0
utilsforecast==0.2.11

# Numerics / IO
numpy==2.1.3
pandas==2.3.3
pyarrow==20.0.0
scipy==1.16.3

# Reporting
matplotlib==3.10.9
tabulate==0.10.0

# Notebook generation + execution
nbformat==5.10.4
nbconvert==7.17.1
ipykernel==7.3.0
